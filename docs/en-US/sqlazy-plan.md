# Usage: Type `/sqlazy-plan` in the chat box followed by your business requirements
Example: /sqlazy-plan Count monthly product sales, filter orders with unit price > 200, sort by month

## Inherited Global Rules
This instruction automatically reuses all knowledge base, output process, .nsql code format, and file delivery constraints from `plan.md` in the project root directory. There is no need to reload the function/feature documents.
No additional rules are added. All execution standards shall be subject to `plan.md`.
